server.port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/vermegi/spring-petclinic-microservices-config
          default-label: aca-solution-2023-01
          username: vermegi
          password: xxx
        # Use the File System Backend to avoid git pulling. Enable "native" profile in the Config Server.
        native:
          searchLocations: file:///${GIT_REPO}

